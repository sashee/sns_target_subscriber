# sns_target_subscriber

* terraform init
* terraform apply
* go to the output url
* send messages targeted to subscriber 1, 2 or both
* observer that only the intended recipient gets the message
* terraform destory
